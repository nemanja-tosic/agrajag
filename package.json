{
  "type": "module",
  "private": true,
  "scripts": {
    "codegen": "yarn workspaces foreach --worktree --topological --parallel --interlaced run codegen",
    "build": "yarn workspaces foreach --worktree --topological --parallel --interlaced run build",
    "build:ts": "cd $INIT_CWD && tsc -b tsconfig.json",
    "publish": "yarn build && yarn workspaces foreach --all --no-private npm publish --tolerate-republish --access public",
    "test": "yarn workspaces foreach --worktree --topological run test",
    "test-e2e": "yarn workspaces foreach --worktree --topological run test-e2e"
  },
  "workspaces": [
    "packages/*",
    "examples/*",
    "test/**"
  ],
  "dependencies": {
    "prettier": "3.2.5",
    "typescript": "5.9.3"
  },
  "resolutions": {
    "inflected": "2.1.0"
  },
  "packageManager": "yarn@4.12.0"
}
